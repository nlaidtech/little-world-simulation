export const CLIMATE_EVENTS = {
  heatwave: {
    id: 'heatwave',
    name: 'Extreme Heatwave Alert',
    icon: '🔥',
    badge: 'Code Red Heat',
    tagline: 'Urban Heat Island amplification threatens public health.',
    description: 'A scorching high-pressure dome has stalled over the city. Asphalt and rooftops are radiating extreme thermal energy. The municipality requires at least 15% tree canopy coverage to shield vulnerable neighborhoods.',
    targetMetric: 'Canopy Coverage ≥ 15%',
    checkSuccess: (results) => (results?.canopy_percent || 0) >= 15.0,
    getProgress: (results) => Math.min(100, Math.round(((results?.canopy_percent || 0) / 15) * 100)),
    grant: 2500,
    damagePenalty: 1200,
    skyTint: 'rgba(230, 80, 20, 0.22)',
    ambientEffect: 'heatwave',
  },
  flood: {
    id: 'flood',
    name: 'Torrential Flash Flood Warning',
    icon: '⛈️',
    badge: 'Storm Surge',
    tagline: 'Record-setting monsoon downpours threaten sewage runoff.',
    description: 'An atmospheric river is inundating the river valley with over 4 inches of rain per hour. Without sufficient tree root systems and canopy interception, raw flood waters will overflow containment levees.',
    targetMetric: 'Stormwater Intercepted ≥ 40,000 m³',
    checkSuccess: (results) => (results?.stormwater_m3 || 0) >= 40000,
    getProgress: (results) => Math.min(100, Math.round(((results?.stormwater_m3 || 0) / 40000) * 100)),
    grant: 3000,
    damagePenalty: 1500,
    skyTint: 'rgba(24, 44, 80, 0.35)',
    ambientEffect: 'flood',
  },
  smog: {
    id: 'smog',
    name: 'Toxic Smog Inversion Episode',
    icon: '🏭',
    badge: 'Air Quality Hazard',
    tagline: 'Winter temperature inversion traps highway PM2.5 particulates.',
    description: 'Stagnant atmospheric pressure has trapped vehicle exhaust and industrial emissions at street level. High-density conifer needle and broadleaf filtration is urgently needed to clear dangerous particulates.',
    targetMetric: 'Pollutants Filtered ≥ 1,500 kg',
    checkSuccess: (results) => (results?.pollutants_kg || 0) >= 1500,
    getProgress: (results) => Math.min(100, Math.round(((results?.pollutants_kg || 0) / 1500) * 100)),
    grant: 2200,
    damagePenalty: 1000,
    skyTint: 'rgba(80, 75, 60, 0.28)',
    ambientEffect: 'smog',
  },
};
